{
  "name": "generator-react-native-redux",
  "version": "0.3.5",
  "description": "Scaffold out a react-native app",
  "scripts": {
    "lint": "yarn-bin-fix && eslint app/index.js test.js",
    "test": "yarn-bin-fix && NODE_ENV=test mocha",
    "prepublish": "npm run lint && npm test"
  },
  "files": [
    "app"
  ],
  "devDependencies": {
    "yarn-bin-fix": "^0.1.15"
  },
  "dependencies": {
    "rimraf": "^2.5.2",
    "semver": "^5.3.0",
    "underscore.string": "^3.0.3",
    "yeoman-generator": "^0.23.3"
  },
  "repository": "https://github.com/lsps9150414/generator-react-native-redux"
}
